enum JobType { fullTime, partTime, internship, freelance, temporary }

enum CourseType { onSite, remote }

enum JobStatus { inProgress, active, ended }

enum JobPerks { activity, workingHours, health, food, commute, entertainment }
